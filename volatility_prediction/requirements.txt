# Volatility Prediction Engine — Dependencies
# Install: pip install -r requirements.txt

# Core
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Data
yfinance>=0.2.0
pyarrow>=14.0.0          # parquet cache for fast data reload

# Deep Learning (LSTM)
tensorflow>=2.13.0       # or: pip install tensorflow-macos (Apple Silicon)

# Gradient Boosting (pick one — LightGBM preferred)
lightgbm>=4.0.0          # fastest, best accuracy
xgboost>=2.0.0           # used in classification backtest

# Optional
matplotlib>=3.7.0        # for static plots
