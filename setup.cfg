[options]
packages = find:
package_dir =
    = .

[options.packages.find]
where = .
include =
    core*
    regime*
    strategy*
    execution*
    risk*
    data*
    backtest*
    utils*
    config*
