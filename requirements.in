# ============================================================
# Trading System — Python Dependencies
# ============================================================

# Core scientific stack
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0

# Market data
yfinance>=0.2.40
ccxt>=4.3.0              # crypto live data + execution

# Charts and reporting
matplotlib>=3.8.0

# Config
pyyaml>=6.0

# Broker adapters
alpaca-trade-api==3.2.0    # Alpaca (US equities — paper + live)
# ib_insync>=0.9.86       # IBKR TWS / IB Gateway (uncomment if using IBKR)
# python-binance>=1.0.19  # Binance spot/futures (uncomment if using Binance)

# Optional: advanced ML signals (uncomment to use)
# scikit-learn>=1.4.0
# xgboost>=2.0.0
# lightgbm>=4.3.0

# Optional: performance analytics
# pyfolio-reloaded>=0.9.5
# quantstats>=0.0.62

# AWS SDK (reporting: S3 upload, SES email)
boto3>=1.34.0
pandas-datareader>=0.10.0  # FRED macro data for EWS
scikit-learn>=1.4.0        # Isolation Forest for EWS anomaly detection

# Utilities
pytz>=2024.1
requests>=2.31.0
tqdm>=4.66.0

# Databento (microstructure signals)
databento>=0.36.0

# Alpaca SDK (microstructure signals + execution)
alpaca-py>=0.25.0

# H2O AutoML (vol forecaster + meta-learner)
# h2o>=3.46.0  # optional: H2O AutoML vol forecaster — heavy dep, not wired in live engine

# Earnings NLP
vaderSentiment>=3.3.2

# Feature store (S3 parquet)
pyarrow>=15.0.0
s3fs==2026.3.0
